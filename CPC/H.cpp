#include <iostream>
using namespace std;
#define cpu()                                                                  \
  ios::sync_with_stdio(false);                                                 \
  cin.tie(nullptr);
#define ll long long
#define lld long double

const int mod = 1e9 + 7;

void solve() {
  int n, k, cant = 0, sep = 0;
  cin >> n >> k;
  string s;
  cin >> s;

  for (int i = 0; i < n; i++) {
  }
}

int main() {
  cpu();
  int t;
  // t=1;
  cin >> t;
  while (t--)
    solve();
  return 0;
}